# 发行包验证记录

日期：2026-09-16。验证环境：原 HP Linux x86_64 工作站。

## 已执行

- 四条随附轨迹的 SHA256 文件检查通过。
- 复制到带空格的另一个目录后，四个 `inspect` 入口和底层服务启动预览通过，未依赖原 `/home/ebim/labs` 路径。
- Bash 脚本语法、主要中文文档链接检查通过。
- `scripts/test.sh` 的 11 项测试通过（17.282 秒）；ROS 集成测试在 `--network none`、domain 93 的容器内运行虚拟控制器。
- 新的 `replay-gui` 镜像构建成功；容器版 `cali` 的 8 秒无窗口读取和 8 秒桌面窗口测试均持续收到真实头部相机图像并正常退出。
- 五个运行镜像导出为 gzip 归档后，重新执行导入；所有 SHA256 和导入后的镜像 ID 核对通过。归档总计 3,966,217,853 字节。
- 非法镜像导入清单在调用 Docker 之前被拒绝。
- 已扫描待发布文件中的常见凭据格式，未发现匹配项。发布内容不包含工作站 SSH 密钥或个人配置。

镜像 ID、架构和大小见 [tested-images.json](tested-images.json)。离线包实际内容以同目录的 `images.json` 和 `SHA256SUMS` 为准。

## 验证边界

- 此次打包验证未发送实机双臂或夹爪运动指令。
- 四个底层服务复用原工作站现有镜像；此次没有在全新机器上从零重新构建全部上游依赖。
- 镜像导入回测使用原机器的 Docker daemon，没有删除正在使用的镜像和容器。
- 尚未在国内网络或其他机器人的硬件上验证；新机器需适配站点参数、系统/FCI 版本及现场环境。
- 软件准备完成不代表已经上传；GitHub 上是否有源码、Release 或镜像附件，应以实际发布结果为准。
